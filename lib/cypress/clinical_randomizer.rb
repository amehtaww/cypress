module Cypress
  class ClinicalRandomizer
    def self.randomize(record, prng = Random.new)

    end

    private

    def self.split_by_date(record, prng)
      new_record = record.clone
      
    end
  end
end
